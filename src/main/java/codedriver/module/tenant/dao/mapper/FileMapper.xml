<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="codedriver.module.tenant.dao.mapper.FileMapper">

	<select id="getFileTypeConfigByType" parameterType="java.lang.String" resultType="codedriver.framework.file.dto.FileTypeVo">
		SELECT
		`name`,
		`config`
		FROM
		`filetype_config`
		WHERE name = #{value}
	</select>

	<insert id="insertFile" parameterType="codedriver.framework.file.dto.FileVo">
		INSERT INTO `file` (
		`uuid`,
		`path`,
		`name`,
		`size`,
		`user_id`,
		`upload_time`,
		`type`
		)
		VALUES
		(
		#{uuid},
		#{path},
		#{name},
		#{size},
		#{userId},
		NOW(),
		#{type}
		)
	</insert>
</mapper>

